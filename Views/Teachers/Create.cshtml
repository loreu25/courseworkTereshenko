@model UniversitySystem2.Models.Teacher
@{
    ViewBag.Title = "Добавить преподавателя";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card mt-4">
    <div class="card-header">Добавить преподавателя</div>
    <div class="card-body">
        <form method="post">
            <div class="mb-3">
                <label class="form-label">ФИО</label>
                <input class="form-control" type="text" name="FullName" value="@Model?.FullName" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Кафедра</label>
                <input class="form-control" type="text" name="Department" value="@Model?.Department" />
            </div>
            <hr />
            <h5>Данные для входа</h5>
            @if (ViewData.ModelState["Login"]?.Errors.Count > 0)
            {
                <div class="alert alert-danger">@ViewData.ModelState["Login"].Errors[0].ErrorMessage</div>
            }
            <div class="mb-3">
                <label class="form-label">Логин</label>
                <input class="form-control" type="text" name="Login" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Пароль</label>
                <input class="form-control" type="password" name="Password" required />
            </div>
            <button class="btn btn-primary" type="submit">Сохранить</button>
            <a class="btn btn-secondary ms-2" href="/Teachers/Index">Назад к списку</a>
        </form>
    </div>
</div>
